--[[
	SimpleAdmin | UI
	- This script just requires the selected UI kit. If you would
	  like to edit the themes, look at the scripts parented to this one.
--]]

local Kit = "Default"

return require(script:WaitForChild(Kit))